main() {
  var hour = DateTime.now().hour;
  print(hour);
}

/* i chose var not const cause var it a variable
and the const its a fixed value
and we cant use a fixed value with datetime
cause it doesn't get the current time. */
